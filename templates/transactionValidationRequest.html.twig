{% extends "transactionSummary.html.twig" %}

{% block title %}Validation commande{% endblock %}



        {% block pageTitle %}
            Vérification de votre commande
        {% endblock %}


        {% block bodyPage %}
            <p id="informationText">Veuillez vérifier votre commande et la valider pour procéder au paiement.</p>


            {{ parent() }}


            <a href="{{ path("cardNumber") }}" class='btn btn btn-info btnSave'>payer</a>
            <a href="{{ path("formTransaction") }}" class='btn btn btn-info btnSave'>Modifier</a>
            <div class="col-lg-12" style="padding-top:20px;padding-left:0;">
                <a style="margin:0;" href='{{ path("index") }}' class='btn btn btn-info btnHome'>
                    Annuler la commande et revenir à l'accueil</a></div>

        {% endblock %}